# Refine `fixed-sleepy-moon-adventure` Template for Sample Image and Category Alignment

## Context

The product roadmap outlines Phase 3 T3-2: "既存10テンプレート品質磨き込み" (refining existing 10 templates). A detailed quality review in `TEMPLATE_QUALITY_REVIEW.md` (as referenced in the T3-2 status update) identified several P1 quality issues. Specifically, the `fixed-sleepy-moon-adventure` template was flagged for "sampleImage 重複・カテゴリ不一致" (duplicate sample image / category mismatch). This task addresses these specific findings to enhance the user experience and visual consistency within the template selection UI.

## Objective

Update the Firestore `FixedStoryTemplate` entry for `fixed-sleepy-
