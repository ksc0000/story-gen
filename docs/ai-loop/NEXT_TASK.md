# Refine `pageVisualRole` Definitions for Enhanced Visual Diversity and Story Pacing

## Context

The product roadmap (Phase 2: Story & Illustration Quality) explicitly calls for `pageVisualRole` improvement to enhance compositional variety and overall image quality. `pageVisualRole` is a critical mechanism for guiding the visual composition of each page, influencing the diversity and narrative pacing of illustrations. With recent foundational improvements like unified image models (PR #375, PR #378) and fixes for style/background consistency (PR #397), we can now focus on fine-tuning `pageVisualRole` to achieve more dynamic and engaging visual storytelling. This task aims to identify and implement specific refinements to `pageVisualRole` definitions, primarily within existing `fixed_template`s or
